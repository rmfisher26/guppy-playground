# Copy this file to .env and fill in values

# URL the frontend uses to reach the API
# In docker compose this is handled automatically via service name
PUBLIC_API_URL=http://localhost:8000

# Optional: set a fixed random seed for reproducible results in tests
# GUPPY_DEFAULT_SEED=42
